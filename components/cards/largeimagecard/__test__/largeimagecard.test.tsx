import { render } from "@testing-library/react";
import React from "react";
import Largeimagecard from "../largeimagecard";

describe("initial render", () => {
  it("should render", () => {
    // const component = render(<Largeimagecard src={''} />)
    // expect(component).toThrowError()
  });
});
